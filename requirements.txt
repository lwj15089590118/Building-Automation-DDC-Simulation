# ============================================================
# 楼宇 DDC 控制与能源管理仿真 —— 第三方依赖清单
# 运行环境：Windows 10 / Python 3.12
# 全部为免费开源库，安装命令：
#   pip install -r requirements.txt
# ============================================================

numpy>=1.26,<3.0        # 数值统计：全天舒适性指标向量化计算(满足率/最大偏差/平均温度)
flask>=3.0,<4.0         # Web 监控看板后端（HTTP + JSON API）
pymodbus>=3.6.9,<4.0    # Modbus/TCP 从站(模拟 DDC 控制器)与主站自测客户端
